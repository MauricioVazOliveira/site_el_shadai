:root {
  --dark: #202020;
  --white: #e3e3e3;
  --accent: #f2a15b;
  --accent-strong: #ffb36b;
  --accent-dark: #b96732;
  --muted: #a9a9a9;
  --card: #292929;
  --card-light: #333333;
  --shadow: 0 24px 70px rgba(0, 0, 0, 0.35);
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  font-family: "Montserrat", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: var(--dark);
  color: var(--white);
}

a {
  color: inherit;
  text-decoration: none;
}

.site-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 50;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px clamp(18px, 4vw, 64px);
  background: transparent;
  border-bottom: none;
  backdrop-filter: none;
}

.brand {
  display: inline-flex;
  align-items: center;
}

.brand-logo {
  width: 94px;
  height: auto;
  display: block;
}

.main-nav {
  display: flex;
  align-items: center;
  gap: 26px;
  font-size: 14px;
  font-weight: 700;
}

.main-nav a {
  color: var(--white);
  opacity: 0.9;
  transition: color 0.2s ease, opacity 0.2s ease;
}

.main-nav a:hover {
  color: var(--accent-strong);
  opacity: 1;
}

.nav-cta {
  padding: 12px 22px;
  border: 1px solid var(--accent);
  border-radius: 999px;
  color: var(--dark) !important;
  background: var(--accent);
  opacity: 1 !important;
}

.nav-cta:hover {
  background: var(--accent-strong);
}

.menu-toggle {
  display: none;
  width: 42px;
  height: 42px;
  border: 1px solid rgba(227, 227, 227, 0.16);
  border-radius: 12px;
  background: transparent;
  cursor: pointer;
  padding: 10px;
}

.menu-toggle span {
  display: block;
  width: 100%;
  height: 2px;
  margin: 5px 0;
  background: var(--white);
  border-radius: 99px;
}

.hero {
  position: relative;
  min-height: 94vh;
  display: grid;
  place-items: center;
  overflow: hidden;
  background:
    radial-gradient(circle at 20% 18%, rgba(242, 161, 91, 0.24), transparent 28%),
    radial-gradient(circle at 80% 78%, rgba(242, 161, 91, 0.17), transparent 30%),
    linear-gradient(135deg, #202020 0%, #171717 52%, #2b211b 100%);
}

.hero-pattern {
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(rgba(227, 227, 227, 0.035) 1px, transparent 1px),
    linear-gradient(90deg, rgba(227, 227, 227, 0.035) 1px, transparent 1px);
  background-size: 42px 42px;
  mask-image: linear-gradient(to bottom, black, transparent 88%);
}

.hero::after {
  content: "";
  position: absolute;
  left: -10%;
  right: -10%;
  bottom: -18%;
  height: 34%;
  background: var(--dark);
  transform: rotate(-3deg);
}

.hero-video {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.55;
  z-index: 1;
}

.hero-content {
  position: relative;
  z-index: 2;
  width: min(980px, 92%);
  padding: 160px 0 90px;
  text-align: center;
  margin: 0 auto;
}

.eyebrow {
  margin: 0 0 16px;
  color: var(--accent);
  font-size: 12px;
  font-weight: 900;
  letter-spacing: 0.22em;
  text-transform: uppercase;
}

.hero h1 {
  margin: 0 0 24px;
  font-size: clamp(48px, 7vw, 80px);
  line-height: 0.9;
  letter-spacing: -0.08em;
  font-weight: 900;
}

.hero-copy {
  display: none;
  max-width: 680px;
  margin: 0 auto 34px;
  color: rgba(227, 227, 227, 0.82);
  font-size: clamp(18px, 2.4vw, 26px);
  line-height: 1.45;
  font-weight: 500;
}

.hero-actions,
.button-row {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 14px;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 52px;
  padding: 15px 26px;
  border-radius: 999px;
  font-weight: 900;
  transition: transform 0.2s ease, background 0.2s ease, color 0.2s ease;
}

.btn:hover {
  transform: translateY(-2px);
}

.btn-primary {
  background: var(--accent);
  color: var(--dark);
}

.btn-primary:hover {
  background: var(--accent-strong);
}

.btn-secondary {
  border: 1px solid rgba(227, 227, 227, 0.28);
  background: rgba(227, 227, 227, 0.08);
  color: var(--white);
}

.btn-outline {
  border: 1px solid rgba(227, 227, 227, 0.28);
  color: var(--white);
}

.quick-links {
  position: relative;
  z-index: 4;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
  width: min(1120px, 92%);
  margin: -82px auto 30px;
}

.quick-links a {
  display: grid;
  gap: 7px;
  padding: 28px;
  border: 1px solid rgba(227, 227, 227, 0.08);
  border-radius: 28px;
  background: linear-gradient(145deg, #292929, #222222);
  box-shadow: var(--shadow);
  transition: transform 0.2s ease, border-color 0.2s ease;
}

.quick-links a:hover {
  transform: translateY(-4px);
  border-color: rgba(242, 161, 91, 0.55);
}

.quick-links span {
  color: var(--accent);
  font-size: 12px;
  font-weight: 900;
}

.quick-links strong {
  font-size: 22px;
  letter-spacing: -0.04em;
}

.quick-links small {
  color: var(--muted);
  font-weight: 600;
}

.section {
  width: min(1160px, 92%);
  margin: 0 auto;
  padding: 90px 0;
}

.welcome,
.message,
.visit {
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  gap: 64px;
}

.section h2 {
  margin: 0 0 24px;
  font-size: clamp(36px, 5vw, 72px);
  line-height: 0.98;
  letter-spacing: -0.07em;
  font-weight: 900;
}

.section p {
  color: rgba(227, 227, 227, 0.72);
  font-size: 18px;
  line-height: 1.7;
}

.check-list {
  display: grid;
  gap: 14px;
  margin: 28px 0;
  padding: 0;
  list-style: none;
}

.check-list li {
  color: var(--white);
  font-weight: 800;
}

.check-list li::before {
  content: "✓";
  margin-right: 12px;
  color: var(--accent);
}

.text-link {
  color: var(--accent-strong);
  font-weight: 900;
}

.image-card {
  position: relative;
  min-height: 520px;
}

.abstract-photo,
.video-card,
.event-image,
.ministry-card {
  background-size: cover;
  background-position: center;
}

.photo-one {
  height: 520px;
  border: 1px solid rgba(242, 161, 91, 0.18);
  border-radius: 38px;
  background:
    radial-gradient(circle at 62% 36%, rgba(242, 161, 91, 0.8) 0 14%, transparent 15%),
    radial-gradient(circle at 30% 72%, rgba(227, 227, 227, 0.12) 0 18%, transparent 19%),
    linear-gradient(135deg, #2f2f2f, #171717 55%, #4b2f1f);
  box-shadow: var(--shadow);
}

.caption-card {
  position: absolute;
  left: -24px;
  bottom: 32px;
  display: grid;
  gap: 5px;
  padding: 22px 26px;
  border: 1px solid rgba(242, 161, 91, 0.25);
  border-radius: 24px;
  background: #252525;
  box-shadow: var(--shadow);
}

.caption-card strong {
  color: var(--white);
}

.caption-card span {
  color: var(--muted);
}

.mission-banner {
  overflow: hidden;
  padding: 28px 0;
  border-top: 1px solid rgba(227, 227, 227, 0.08);
  border-bottom: 1px solid rgba(227, 227, 227, 0.08);
  background: #151515;
  color: var(--accent);
}

.marquee {
  display: flex;
  gap: 34px;
  width: max-content;
  white-space: nowrap;
  animation: scroll 24s linear infinite;
  font-size: clamp(26px, 4vw, 54px);
  font-weight: 900;
  letter-spacing: -0.06em;
}

@keyframes scroll {
  from { transform: translateX(0); }
  to { transform: translateX(-50%); }
}

.video-card {
  display: grid;
  place-items: center;
  height: 420px;
  border: 1px solid rgba(242, 161, 91, 0.18);
  border-radius: 38px;
  background:
    linear-gradient(135deg, rgba(0, 0, 0, 0.14), rgba(0, 0, 0, 0.62)),
    radial-gradient(circle at 35% 40%, rgba(242, 161, 91, 0.92) 0 15%, transparent 16%),
    linear-gradient(135deg, #383838, #171717 55%, #4b2f1f);
  box-shadow: var(--shadow);
}

.play-button {
  width: 88px;
  height: 88px;
  border: 0;
  border-radius: 50%;
  background: var(--accent);
  color: var(--dark);
  box-shadow: var(--shadow);
  font-size: 30px;
  cursor: pointer;
}

.section-heading {
  max-width: 760px;
  margin: 0 auto 42px;
  text-align: center;
}

.event-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.event-card {
  overflow: hidden;
  border: 1px solid rgba(227, 227, 227, 0.08);
  border-radius: 34px;
  background: var(--card);
  box-shadow: 0 16px 45px rgba(0, 0, 0, 0.24);
}

.event-image {
  height: 230px;
}

.img-a {
  background: linear-gradient(135deg, #151515, #f2a15b);
}

.img-b {
  background: linear-gradient(135deg, #202020, #7f583f, #f2a15b);
}

.img-c {
  background: linear-gradient(135deg, #f2a15b, #2c2c2c);
}

.event-body {
  padding: 24px;
}

.event-body p {
  margin: 0 0 8px;
  color: var(--accent);
  font-size: 13px;
  font-weight: 900;
  letter-spacing: 0.14em;
  text-transform: uppercase;
}

.event-body h3 {
  margin: 0 0 18px;
  font-size: 26px;
  letter-spacing: -0.05em;
}

.event-body a {
  color: var(--accent-strong);
  font-weight: 900;
}

.split-cards {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
}

.ministry-card {
  display: flex;
  align-items: flex-end;
  height: 320px;
  padding: 32px;
  border: 1px solid rgba(242, 161, 91, 0.18);
  border-radius: 38px;
  color: var(--white);
  box-shadow: var(--shadow);
  font-size: 44px;
  font-weight: 900;
  letter-spacing: -0.07em;
}

.kids {
  background:
    linear-gradient(135deg, rgba(0,0,0,0.12), rgba(0,0,0,0.52)),
    linear-gradient(135deg, #f2a15b, #6f3d22);
}

.students {
  background:
    linear-gradient(135deg, rgba(0,0,0,0.12), rgba(0,0,0,0.52)),
    linear-gradient(135deg, #2f2f2f, #8d5b39, #f2a15b);
}

.visit {
  margin-bottom: 70px;
  padding: 54px;
  border: 1px solid rgba(242, 161, 91, 0.18);
  border-radius: 42px;
  background: linear-gradient(145deg, #292929, #1b1b1b);
}

.visit-details {
  padding: 28px;
  border: 1px solid rgba(227, 227, 227, 0.08);
  border-radius: 28px;
  background: #202020;
}

.visit-details p {
  margin: 0 0 14px;
}

.visit-details strong {
  color: var(--accent);
}

.site-footer {
  padding: 58px clamp(22px, 5vw, 70px) 28px;
  border-top: 1px solid rgba(227, 227, 227, 0.08);
  background: #151515;
  color: var(--white);
}

.footer-brand {
  display: flex;
  align-items: center;
  gap: 22px;
  margin-bottom: 42px;
}

.footer-logo {
  width: 120px;
  height: auto;
}

.footer-brand p {
  max-width: 420px;
  color: rgba(227, 227, 227, 0.72);
  line-height: 1.6;
}

.footer-links {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
}

.footer-links div {
  display: grid;
  gap: 12px;
}

.footer-links h4 {
  margin: 0 0 8px;
  color: var(--accent);
}

.footer-links a {
  color: rgba(227, 227, 227, 0.78);
}

.footer-links a:hover {
  color: var(--accent-strong);
}

.copyright {
  margin: 42px 0 0;
  padding-top: 22px;
  border-top: 1px solid rgba(227, 227, 227, 0.1);
  color: rgba(227, 227, 227, 0.58);
}

@media (max-width: 900px) {
  .brand-logo {
    width: 82px;
  }

  .menu-toggle {
    display: block;
  }

  .main-nav {
    position: absolute;
    top: 82px;
    left: 16px;
    right: 16px;
    display: none;
    flex-direction: column;
    align-items: flex-start;
    gap: 18px;
    padding: 22px;
    border: 1px solid rgba(227, 227, 227, 0.1);
    border-radius: 24px;
    background: #202020;
    box-shadow: var(--shadow);
  }

  .main-nav.open {
    display: flex;
  }

  .quick-links,
  .welcome,
  .message,
  .event-grid,
  .split-cards,
  .visit,
  .footer-links {
    grid-template-columns: 1fr;
  }

  .quick-links {
    margin-top: -52px;
  }

  .hero h1 {
    font-size: 64px;
  }

  .section {
    padding: 62px 0;
  }

  .caption-card {
    left: 18px;
  }

  .visit {
    padding: 34px 24px;
  }

  .footer-brand {
    align-items: flex-start;
    flex-direction: column;
  }
}

@media (max-width: 520px) {
  .site-header {
    padding: 12px 16px;
  }

  .brand-logo {
    width: 74px;
  }

  .hero-content {
    padding-top: 140px;
  }

  .hero h1 {
    font-size: 54px;
  }

  .section h2 {
    font-size: 40px;
  }

  .quick-links a,
  .event-body,
  .visit-details {
    padding: 22px;
  }
}

 
 . h e r o - v i d e o   { 
     p o s i t i o n :   a b s o l u t e ; 
     t o p :   0 ; 
     l e f t :   0 ; 
     w i d t h :   1 0 0 % ; 
     h e i g h t :   1 0 0 % ; 
     o b j e c t - f i t :   c o v e r ; 
     o p a c i t y :   0 . 4 ; 
 } 
 
 